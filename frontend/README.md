# AthsVic Insights (frontend)

React + TypeScript shell for the analytics dashboard.

## Run

```
npm install
npm run dev
```

Open http://localhost:5173 (API proxy to Django on :8000 when you add data fetching).
